from the following website:  http://www.mkyong.com/spring-batch/spring-batch-example-xml-file-to-csv-file/

except it uses Maven whereas this uses Gradle.  I've had problems with Maven, particularly the Eclipse
plugin and Maven projects in Eclipse.

make src/resources a source folder (that is, one that holds packages).  That resolves spring "does not exist" issue